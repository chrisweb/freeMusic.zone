set up route 53 for a domain

create an EC2 instance
install nodejs
checkout project from github
install bower and npm dependencies
run the deployment script (grunt)

enable elasctic load balancer for the nodejs ec2 instances

tell route53 to send the traffic to the elastic load balancer  

enable autoscaling for the ec2 instance so that another instance gets created automatically if the resources are exhausted
(destroy instances if they are not needed anymore)

create two ec2 instances for a redis cluster
add a password for the redis cluster
put the redis cluster ip and password into the nodejs project configuration

create two ec2 instances for a mongodb cluster
add a password for the mongodb cluster
put the mongodb cluster ip and password into the nodejs project configuration